const CubeTitle = ({ title }: { title: string }) => {
	return (
		<div className="my-4">
			<h4 className="text-small font-semibold leading-none mx-5">{title}</h4>
		</div>
	);
};

export default CubeTitle;
